import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.parlourease.client',
  appName: 'Parlour Ease Client',
  webDir: 'out'
};

export default config;
