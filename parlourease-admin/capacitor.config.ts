import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.parlourease.admin',
  appName: 'Parlour Ease Admin',
  webDir: 'out'
};

export default config;
